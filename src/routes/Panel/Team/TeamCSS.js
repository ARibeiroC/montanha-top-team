import styled from "styled-components";

export const Container = styled.div`
    display: flex;

    height: 85vh;
    padding: 1rem;
    background: var(--color-darkgray);
    color: var(--color-ice);
`